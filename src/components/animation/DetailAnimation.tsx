import styled from "styled-components";

function DetailAnimation() {
  return <Container>DetailAnimation</Container>;
}

const Container = styled.div`
  background: rgba(256, 256, 256, 0.2);
  width: 100%;
  height: 400px;
`;

export default DetailAnimation;
